const config = {
  server: {
    protocol: 'http',
    hostname: 'localhost',
    port: '4000'
  },
  cadence: {
    protocol: 'http',
    hostname: 'localhost',
    port: '5000',
  }
};

export default config;
